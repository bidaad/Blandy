export interface VwAvailDeliveryDate {
  id: string;
  code: string;
  sign: string;
  fromDate: Date;
  toDate: Date;
  isActive: boolean;
  departmentSign: string;
  weekDay: string;
  daySign: string;
  departmentCode: string;
}